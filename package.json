{
  "name": "orch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "oxlint src/",
    "lint:fix": "oxlint --fix src/",
    "fmt": "oxfmt --write src/",
    "fmt:check": "oxfmt --check src/",
    "check": "npm run fmt:check && npm run lint && npm run test",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "bundle": "npm run build && rm -rf ~/.claude/orch && mkdir -p ~/.claude/orch/js ~/.claude/orch/skills && rsync -a --include='*/' --include='*.js' --exclude='*' dist/ ~/.claude/orch/js/ && cp -r skills/* ~/.claude/orch/skills/ && ln -s $(pwd)/node_modules ~/.claude/orch/js/node_modules && echo 'Bundled to ~/.claude/orch/'"
  },
  "dependencies": {
    "ink": "^6.8.0",
    "react": "^19.2.4",
    "zod": "^3"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@types/react": "^19.2.14",
    "ink-testing-library": "^4.0.0",
    "oxfmt": "^0.42.0",
    "oxlint": "^1.57.0",
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
